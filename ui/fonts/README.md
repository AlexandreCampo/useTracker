Liberation Sans Regular and Liberation Mono Regular, version 2.1.5, are bundled
unmodified from the Liberation Fonts distribution. See `OFL.txt` for copyright
and the SIL Open Font License 1.1.

CMake embeds these files in the executable, so packaged and standalone builds
use the same typography without depending on installed system fonts. The license
is included in installed packages.
